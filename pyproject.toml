[project]
name = "spreadsheets-challenge"
version = "0.1.0"
description = "Parser for a pipe-delimited CSV with excel-like formulas"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    # 7.10.7 is the newest coverage that still supports python 3.9
    "coverage==7.10.7",
    "parameterized==0.9.0",
]

[tool.uv]
package = false
